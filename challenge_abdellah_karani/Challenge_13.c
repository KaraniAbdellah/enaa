#include <stdio.h>

int main() {
    int n, octal = 0, hex = 0, divide, remainder, base = 1, temp;

    printf("Enter a Number: ");
    scanf("%d", &n);


	printf("Octal: %o\n", n);
    printf("Hexadecimal: %X\n", n);

    return 0;
}










